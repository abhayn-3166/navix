# NAVIX

Python-only supply-chain resilience hackathon prototype. It uses the Python standard library and browser-side JavaScript for the interactive demo state.

Run locally:

```powershell
python app.py
```

Then open `http://127.0.0.1:8000`.
